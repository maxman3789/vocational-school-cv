<div class="container">
    {{#if removedUser}}
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        Removed!
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    {{/if}}
    <div class="row">

        <div class="col-6">
            <h1>Staff</h1>
        </div>
        <div class="col-6 d-flex justify-content-end">
            <a href="/addstaff" type="button" class="btn btn-primary align-self-center">+ Add Staff</a>
        </div>
    </div>
    <div class="row">
        <table>
            <thead class="thead dark">
                <tr>
                    <th scope="col">
                        <form class="d-flex" method="POST" action="/staff" novalidate>
                            <input class="form-control me-2" type="search" placeholder="Search" name="search"
                                aria-label="Search">
                            <button class="btn btn-outline-success" type="submit">Search</button>
                        </form>
                    </th>
                </tr>
            </thead>
        </table>
        <table class="table table-bordered">
            <thead class="thead dark">
                <tr>
                    {{!-- <th scope="col">#</th> --}}
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Phone</th>
                    <th scope="col">Designation</th>
                    <th scope="col" class="text-end">Action</th>
                    
                    {{!-- <th scope="col" class="text-end">Delete</th> --}}
                </tr>
            </thead>
            <tbody>
                {{#each rows}}
                <tr>
                    {{!-- <td scope="row">{{this.id}}</td> --}}
                    <td>{{this.first_name}} {{this.middle_name}} {{this.last_name}}</td>
                    <td>{{this.email}}</td>
                    <td>{{this.phone}}</td>
                    <td>{{this.designation}}</td>
                    <td class="text-end">
                        <a href="/viewstaff/{{this.id}}" type="button" class="btn btn-light btn-small"><i
                                class="bi bi-eye"></i> View</a>
                        <a href="/editstaff/{{this.id}}" type="button" class="btn btn-light btn-small"><i
                                class="bi bi-pencil"></i>
                            Edit</a>
                    </td>
                    {{!-- <td class="text-end">
                        <a href="/delete/{{this.id}}" type="button" class="btn btn-light btn-small"><i
                                class="bi bi-person-x"></i> Delete</a>
                    </td> --}}
                </tr>
                {{/each}}
            </tbody>
        </table>
        
        
    </div>
</div>