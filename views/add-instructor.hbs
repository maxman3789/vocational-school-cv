<div class="container">
    <div class="row">
        <h2>New Instructor</h2>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/instructor">Instructor</a></li>
                <li class="breadcrumb-item active" aria-current="page">New Instructor</li>
            </ol>
        </nav>
        <hr>
        {{#if alert }}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            {{alert}}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        {{/if}}
        <form class="row g-3 needs-validation" method="POST" action="/addinstructor" novalidate>
            {{> instructor-form}}
        </form>
    </div>
</div>